{
  "name": "pa-social",
  "version": "0.1.0",
  "private": true,
  "description": "Republication automatique des articles Passion Aquitaine en carrousel Instagram",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "smoke": "node --env-file=.env scripts/smoke-ig.mjs",
    "run": "node --env-file=.env src/index.mjs",
    "test": "node --test \"tests/*.test.mjs\"",
    "preview": "node --env-file=.env scripts/preview.mjs",
    "fixtures": "node scripts/capture-fixtures.mjs"
  },
  "dependencies": {
    "basic-ftp": "^5.3.1",
    "fast-xml-parser": "^5.2.0",
    "playwright": "^1.55.0",
    "sharp": "^0.32.6"
  }
}
